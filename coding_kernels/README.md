# Coding Kernels

These files are the compact local language references that AEGIS injects during coding turns and program-build loops.

- Keep each file terse and implementation-focused.
- Prefer syntax, structure, verification, and common failure patterns.
- Avoid essays, tutorials, or repeated prose.
- Add new languages by creating `<language>.txt` and registering the profile in `coding_kernels.py`.
