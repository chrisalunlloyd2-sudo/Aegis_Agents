Rules:
- Never hard-code passwords, API keys, tokens, or email credentials.
- Prefer environment variables, ignored local config files, or OS credential storage.
- Keep examples fake and clearly marked.
- For local automation, separate secret loading from business logic.

Verification:
- Confirm generated code references secret names, not secret values.
- Tests should use fake credentials and mocks.
